if XDEVMENUBUILD
 xdev_dir=xdevmenu
else
 xdev_dir=
endif

if ALARMBUILD
 alarm_dir=alarm
else
 alarm_dir=
endif

if DC_BUILD
  dc_dir=dc
else
  dc_dir=
endif

SUBDIRS = . manager msg $(dc_dir) dbase starter $(xdev_dir) $(alarm_dir)

